# Use the run script to run these examples, along with the default
# Configuration!
x = [1, 2, 3, 4]
y = [1, 3, 2, 4]

plt.plot(x, y)
plt.xlabel("X-axis")
plt.ylabel("Y-axis")
plt.title("QuickerMD's Awesome Plot")

plt.show()
